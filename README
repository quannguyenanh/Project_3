tail -n number filename  <-- in ra màn hình number dòng cuối cùng của file filename
tail -f filename <-- in ra màn hình 10 dòng cuối cùng của file filename, đồng thời follow file đó realtime, khi có dữ liệu mới chèn vào file thì đồng thời in ra màn hình phần dữ liệu mới đó.

